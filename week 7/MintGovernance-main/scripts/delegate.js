const { ethers } = require("hardhat");

async function main() {
  //in the mint function the owner already gets the tokens
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
